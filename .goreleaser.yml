# generated by: yake code goreleaser
version: 2
dist: _dist_build/
before:
  hooks:
    - go mod tidy
builds:
  - id: nat-pmp
    binary: nat-pmp
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
upx:
  - enabled: true
    goos:
      - linux
    goarch:
      - amd64
      - arm64
nfpms:
  - id: nat-pmp
    package_name: nat-pmp
    vendor: vitalvas
    homepage: https://github.com/vitalvas/nat-pmp
    maintainer: vitalvas <vitalvas@users.noreply.github.com>
    description: nat-pmp command line tool
    license: Unknown
    formats:
      - deb
    section: utils
    priority: optional
    contents:
      - src: packaging/nat-pmp.service
        dst: /lib/systemd/system/nat-pmp.service
      - dst: /etc/nat-pmp
        type: dir
        file_info:
          mode: 0755
    scripts:
      postinstall: packaging/postinstall.sh
checksum:
  name_template: checksums.txt
  algorithm: sha256
snapshot:
  version_template: '{{ .Version }}-{{ .CommitTimestamp }}-{{ .ShortCommit }}'
changelog:
  disable: true
release:
  github:
    owner: vitalvas
    name: nat-pmp
  draft: false
  prerelease: auto
  mode: keep-existing
